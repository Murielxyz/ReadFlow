# 2026-07-09 开发日志

## 完成：Phase 16-28 — 项目全面增强与完善

本篇覆盖 2026-07-04 至 2026-07-09 期间完成的 13 个阶段。

---

### Phase 16：3 核心页面增强 ✅

**目标**：重写 Library、Discover（后合并入 Library）、Statistics 三个核心页面。

**主要变更**：
- **Library**：书架/书单双 Tab、筛选面板、排序面板、导入弹窗、长按菜单
- **Discover**：搜索、推荐、手动添加、最近加入书架（功能合并入 Library 的 + 按钮菜单）
- **Statistics**：周/月/年/总 4 Tab、5 统计卡片、日历热力图、目标管理
- 新增 `ImportModal`、`ManualAddModal`、`AddBookMenuSheet` 组件
- `useStatistics` Hook 全面扩展（年度/月度/周度）

---

### Phase 17：阅读计时增强 ✅

**目标**：后台计时补偿 + 停止弹窗（页数/章节/读完标记）+ 手动记录增强。

**主要变更**：
- 数据库迁移 v2：`reading_sessions` & `manual_logs` 加 `page_number`/`chapter`/`completed_book`
- AppState 后台计时补偿（`backgroundedAt` 机制）
- `StopTimerSheet` 停止计时弹窗（页数/章节/读完标记）
- `ManualLogSheet` 增强：页数/章节/读完 toggle
- "继续阅读" vs "开始阅读" 动态文案 + Reader 进度预填
- `ReadingTimeline` 展示页数/读完标签

---

### Phase 18：笔记系统 ✅

**目标**：添加阅读笔记功能，支持添加/编辑/删除。

**主要变更**：
- 数据库迁移 v4：`notes` 表（`book_id` FK CASCADE + 索引）
- Note 数据模型 + `useNoteStore`（CRUD + 乐观更新）
- `AddNoteSheet` 组件（底部弹窗，新建/编辑双模式）
- 书籍详情页集成笔记区（书单与时间线之间）
- `useStatistics` 接入真实笔记数量

---

### Phase 19：每日阅读提醒 ✅

**目标**：每天定时推送阅读提醒通知。

**主要变更**：
- 安装 `expo-notifications` + 配置 `app.json`（插件 + Android 通知频道）
- 数据库迁移 v5：`user_settings` 表（key-value 持久化存储）
- 扩展 `useSettingsStore`（`reminderEnabled` / `reminderTime` / 持久化到 SQLite）
- 创建 `notificationService`（权限请求 / 调度 / 取消 / 更新）
- Profile 页面新增"每日阅读提醒"设置行 + 时间选择 Modal
- 应用启动时恢复提醒调度（`_layout.tsx`）

---

### Phase 20：设置持久化与应用打磨 ✅

**目标**：修复用户偏好重启后丢失的 Bug，激活 Profile 功能按钮。

**主要变更**：
- 持久化每日阅读目标（`setDailyGoal` 写入 `user_settings`）
- 持久化主题模式（`useThemeStore` 新增 `loadTheme`）
- 激活 Profile "给我们评分"和"反馈与帮助"按钮
- 清理死代码（`mockData.ts` 删除未使用 exports，删除 `BookFilters.tsx`）

---

### Phase 21：高亮系统 ✅

**目标**：阅读高亮功能，支持颜色选择 + 附加笔记。

**主要变更**：
- Highlight 数据模型 + 数据库迁移 v6：`highlights` 表
- `useHighlightStore`（CRUD + 乐观更新 + 失败回滚）
- `AddHighlightSheet` 组件（颜色选择器 4 色 + 可选 note 字段 + 页码/章节）
- 书籍详情页集成高亮区
- Statistics 接入真实 `highlightsCount`

---

### Phase 22：Bug 修复 & 版本号更新 ✅

**目标**：修复标签筛选不生效、长按菜单静默失败 Bug。

**主要变更**：
- `useTagStore` 新增 `bookTagMap` + `fetchAllBookTagIds()`
- Library 标签筛选 OR 逻辑修复
- 长按菜单「加入书单」Bug 修复（`contextBookId` 提前清空问题）
- 版本号 v2.0.0 → v2.2

---

### Phase 23：书单导航 + 统计修复 + 死代码清理 ✅

**目标**：书单卡片可点击、统计标签计数修复。

**主要变更**：
- `useCollectionStore` 新增 `bookCollectionMap` + `fetchAllBookCollectionIds()`
- 书单卡片点击：设置筛选 + 切换到书架 Tab
- 筛选激活时显示紫色提示条
- `book/[id].tsx` 标签/书单计数修复
- `useBookStore` 移除未使用的方法（40+ 行死代码）

---

### Phase 24：Profile 占位符修复 + 用户个性化 ✅

**目标**：支持用户编辑昵称和个性签名。

**主要变更**：
- `useSettingsStore` 新增 `userName`/`userBio` + SQLite 持久化
- 用户卡片可点击 → 编辑弹窗（昵称 + 个性签名 TextInput）
- 头像字母随昵称动态变化
- `handleRateUs` 改为感谢弹窗
- `handleFeedback` 改为友好提示弹窗

---

### Phase 25：Reader 文字选中高亮 ✅

**目标**：EPUB 阅读器中选中文字 → 一键填入高亮。

**主要变更**：
- `AddHighlightSheet` 新增 `prefillContent`/`prefillChapter` props
- EPUB 模板新增 `setupTextSelection()` JS + 浮动高亮按钮
- Reader 页面处理 `highlight` 消息 + 嵌入高亮弹窗
- iframe 重建兼容（翻页后按钮保持可用）

---

### Phase 26：备份/恢复补全 ✅

**目标**：修复导出/恢复功能的数据完整性问题。

**主要变更**：
- 修复 4 张表的恢复 INSERT 列名匹配
- 新增 8 张表导出（tags / book_tags / collections / book_collections / reading_goals / notes / user_settings / highlights）
- 新增 8 张表恢复（含适当去重策略）
- 版本号 v2.2 → v2.3

---

### Phase 27：书单创建跨平台 + 编辑 ✅

**目标**：修复 Android 无法创建书单的 bug（`Alert.prompt` iOS-only）。

**主要变更**：
- 新建 `CreateCollectionSheet.tsx`（底部弹窗，支持创建 + 编辑模式）
- `useCollectionStore` 新增 `updateCollection` 方法
- Library `Alert.prompt` → `CreateCollectionSheet`

---

### Phase 28：书单详情页 ✅

**目标**：创建独立的书单详情页，替代原先的内联筛选方式。

**主要变更**：
- `useCollectionStore` 新增 `fetchBooksInCollection` + `collectionBooks` 状态
- 新建 `app/collection/[id].tsx`（头部卡片 + 操作按钮 + 书籍网格 + 空状态）
- `_layout.tsx` 注册 `collection/[id]` 路由
- `library.tsx` 书单卡片导航改为 `router.push`

---

## 新增文件总览

| # | 文件 | 说明 |
|---|------|------|
| 1 | `src/components/library/ImportModal.tsx` | 导入本地文件弹窗 |
| 2 | `src/components/library/ManualAddModal.tsx` | 手动添加弹窗 |
| 3 | `src/components/library/AddBookMenuSheet.tsx` | 添加书本菜单 |
| 4 | `src/components/library/CreateCollectionSheet.tsx` | 创建/编辑书单弹窗 |
| 5 | `src/components/book/AddNoteSheet.tsx` | 笔记编辑弹窗 |
| 6 | `src/components/book/AddHighlightSheet.tsx` | 高亮编辑弹窗 |
| 7 | `src/components/book/ReadingTabsView.tsx` | 详情页多 Tab 视图 |
| 8 | `src/components/reader/StopTimerSheet.tsx` | 停止计时弹窗 |
| 9 | `src/components/library/BookMenuSheet.tsx` | 书本长按菜单 |
| 10 | `src/components/statistics/StatCards.tsx` | 统计卡片组件 |
| 11 | `src/components/statistics/MiniCalendar.tsx` | 迷你日历热力图 |
| 12 | `src/components/statistics/TimeFilter.tsx` | 时间筛选器 |
| 13 | `src/services/notificationService.ts` | 通知服务 |
| 14 | `src/stores/useNoteStore.ts` | 笔记状态管理 |
| 15 | `src/stores/useHighlightStore.ts` | 高亮状态管理 |
| 16 | `app/search.tsx` | 独立搜索页 |
| 17 | `app/timer.tsx` | 全屏计时器 |
| 18 | `app/my-reading.tsx` | 我的阅读统计详情 |
| 19 | `app/collection-manage.tsx` | 书单管理页 |
| 20 | `app/collection/[id].tsx` | 书单详情页 |

---

## 验证

- ✅ TypeScript 编译零错误（全部阶段）
- ✅ 所有 28 个 Phase 标记完成
- ✅ Git 工作树干净

## 今日维护（2026-07-09）

- 🔧 版本号统一：`app.json` 2.2 → 2.3，`CLAUDE.md` v3.0 → v2.3
- 🧹 清理空目录 `src/components/discover/`
- 📝 更新 `requirements.md`：Discover Tab → 合并入 Library，新增 5 个子页面
- 📝 更新 `architecture.md`：路由结构同步实际实现
- 📝 补写本文档（Phase 16-28 汇总）
